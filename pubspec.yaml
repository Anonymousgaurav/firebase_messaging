name: firebase_messaging_demo
description: A new Flutter project based on firebase push notification

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_messaging: ^11.2.7
  build_runner: ^2.1.2
  json_serializable: ^5.0.2
  flutter_local_notifications:
  http:


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

